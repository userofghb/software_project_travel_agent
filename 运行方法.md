安装依赖  pip install -r backend\requirements.txt

建议复制.env.example,并将复制的文件名字改为.env,修改为自己的密码等内容

运行数据库建库、建表、导入数据脚本  python backend\scripts\init_mysql.py

启动后端：
cd .\backend\
uvicorn app.main:app --reload --port 8000

启动前端：
另开一个终端
cd .\frontend\
npm install
npm run dev